# Onda — Dependencias fijadas para inferencia (torch 2.14.0; el wheel CUDA lo instala
# entrypoint.sh en runtime dentro de /opt/pytorch-backends/<gpu>).
# TODAS las versiones estan fijadas y coinciden con el estado REAL instalado en la imagen validada onda:v3.5.0-test.
# Inventario exacto de referencia (pip freeze): requirements.lock
# Verificacion: tools/check-deps.sh

# -- Core científico --
numpy==2.4.6
scipy==1.18.1
soundfile==0.14.0
librosa==1.0.0

# -- PyTorch ecosystem --
einops==0.8.2
PyYAML==6.0.3
psutil==7.2.2
pydub==0.25.1
resampy==0.4.3

# -- Demucs (instalado aparte con --no-deps) --
sphn==0.2.1
omegaconf==2.3.1
# NOTA: diffq y torchcodec se retiran en v3.5.0: diffq solo sirve para modelos
# cuantizados de demucs (extra 'quantized') que no usamos, y torchcodec no es
# requerido por ningun paquete ni importado por Onda.
pytorch_lightning==2.6.6
ml_collections==1.1.0

# -- ONNX --
onnx==1.23.0
# onnxruntime-gpu NO esta en site-packages: lo instala entrypoint.sh en /opt/pytorch-backends/<gpu>.
# Version instalada verificada en el volumen: 1.26.0 (por eso no aparece en requirements.lock).
onnxruntime-gpu==1.26.0
onnx2pytorch==0.6.0

# -- Modelos específicos --
beartype==0.22.9
rotary_embedding_torch==0.9.1
segmentation_models_pytorch==0.5.0
transformers==5.17.0
spafe==0.3.3
# audiomentations: declarado historicamente pero NO esta instalado en onda:v3.4.14
# (`pip show audiomentations` -> "Package(s) not found") y ningun codigo del repo lo importa.
# Se deja comentado para que no quede una linea sin fijar. Si vuelve a hacer falta, fijarlo a la
# version realmente instalada (no inventarla).
# audiomentations
# NOTA: asteroid/openunmix/torch_audiomentations se han retirado en v3.5.0 porque
# arrastran torchaudio; demucs 4.1.0 no lo requiere.
julius==0.2.8
samplerate==0.2.4
dora-search==0.1.13
lameenc==1.8.4
