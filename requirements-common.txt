# Onda — Dependencias comunes (sin torch). Las instala el Dockerfile (stage python-base).
# TODAS las versiones estan fijadas y coinciden con el estado REAL instalado en la imagen validada onda:v3.5.6.
# Inventario exacto de referencia (pip freeze): requirements.lock
# Verificacion: tools/check-deps.sh

numpy==2.5.3
scipy==1.18.1
soundfile==0.14.0
librosa==1.0.0
einops==0.8.2
PyYAML==6.0.3
psutil==7.2.2
omegaconf==2.3.1
onnx==1.23.0
beartype==0.22.9
lameenc==1.8.4
