# Onda Docker (ROCm / AMD) — Requisitos minimos para inferencia.
# torch + torchaudio + torchvision los aporta la imagen base ROCm (ver Dockerfile.amd).
#
# ⚠️  RUTA NO VALIDADA
#   Esta ruta (ROCm/AMD) NO se ha construido, desplegado ni probado en esta campana.
#   Las versiones de abajo se han ALINEADO con las de `requirements-docker.txt` (la ruta CUDA,
#   validada contra la imagen onda:v3.4.14) SIN haberlas probado en un entorno AMD.
#   Al validar de verdad la ruta ROCm: construir, y despues
#       tools/check-deps.sh <imagen-rocm>
#   y corregir lo que salga. Inventario de referencia de la ruta CUDA: requirements.lock

# -- Core científico --
numpy==2.4.6
scipy==1.17.1
soundfile==0.13.1
librosa==0.11.0

# -- PyTorch ecosystem --
einops==0.8.2
PyYAML==6.0.3
psutil==7.2.2
pydub==0.25.1
resampy==0.4.3

# -- Demucs --
demucs==4.0.1
torchcodec==0.12.0
diffq==0.2.4
omegaconf==2.3.0
pytorch_lightning==2.6.6
ml_collections==1.1.0

# -- ONNX --
onnx==1.21.0
# onnxruntime-gpu: en la ruta CUDA lo instala entrypoint.sh en /opt/pytorch-backends/<gpu>
# (1.26.0 verificado). Aqui se mantiene la misma version por alineacion.
onnxruntime-gpu==1.26.0
onnx2pytorch==0.6.0

# -- Modelos específicos --
beartype==0.22.9
rotary_embedding_torch==0.9.1
segmentation_models_pytorch==0.5.0
transformers==5.17.0
spafe==0.3.3
# audiomentations: NO esta instalado en la imagen validada (`pip show` -> not found) y ningun
# codigo lo importa. Se deja comentado para no fijar una version inventada.
# audiomentations
torch_audiomentations==0.12.0
asteroid==0.7.0
julius==0.2.8
samplerate==0.2.4
# Antes aparecia como `Dora` (nombre incorrecto): el paquete real instalado es `dora-search`.
# Alineado con requirements-docker.txt -> 0.1.13.
dora-search==0.1.13
